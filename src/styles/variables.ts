
const mainColor = '#005D74';
const white = '#fff';
const black = '#333';

export const button = {
	default: {
		bg: white,
		text: mainColor,
		border: mainColor,
	},
	hover: {
		bg: mainColor,
		text: white,
		border: mainColor,
	}
}

export const text = {
	default: black,
} 